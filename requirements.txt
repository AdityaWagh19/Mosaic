# Mosaic — Python dependencies (pinned versions for reproducibility)
#
# Installation
# ------------
# Standard packages:
#   pip install -r requirements.txt
#
# PyTorch CPU build (Windows/Linux without CUDA):
#   pip install torch==2.1.2+cpu --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric==2.4.0
#
# If you have a CUDA GPU, replace torch==2.1.2+cpu with torch==2.1.2
# and install the matching CUDA toolkit build from pytorch.org.

# ---------------------------------------------------------------------------
# Core simulation
# ---------------------------------------------------------------------------
mesa==2.1.5
networkx==3.2.1
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2

# ---------------------------------------------------------------------------
# Visualisation
# ---------------------------------------------------------------------------
matplotlib==3.8.2
seaborn==0.13.2
pillow==10.2.0

# ---------------------------------------------------------------------------
# Machine learning
# PyTorch: install manually with the CPU or CUDA wheel — see header above.
# ---------------------------------------------------------------------------
# torch==2.1.2+cpu          # install separately — see above
# torch-geometric==2.4.0    # install after torch
umap-learn==0.5.5

# ---------------------------------------------------------------------------
# API backend
# ---------------------------------------------------------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
pydantic==2.5.3

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==7.4.4
pytest-cov==4.1.0

# ---------------------------------------------------------------------------
# Notebooks
# ---------------------------------------------------------------------------
jupyter==1.0.0
ipykernel==6.28.0
