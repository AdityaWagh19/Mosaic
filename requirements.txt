# Mosaic — Python dependencies (pinned versions for reproducibility)
#
# GPU (CUDA 12.1) Installation — NVIDIA driver >= 527.41 required
# Tested on: NVIDIA GeForce GPU, driver 529.04, CUDA 12.0 (cu121 compatible)
#
# Note: torch==2.1.2+cu121 does not exist (cu121 wheel server starts at 2.2.0).
#       Mosaic uses torch==2.4.1+cu121 + torch-geometric==2.6.1.
# ---------------------------------------------------------------
# Step 1 — PyTorch + torchvision (CUDA 12.1):
#   pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
# Step 2 — PyTorch Geometric + sparse extensions:
#   pip install torch-geometric==2.6.1
#   pip install torch-scatter torch-sparse torch-cluster torch-spline-conv \
#       -f https://data.pyg.org/whl/torch-2.4.1+cu121.html
#
# Step 3 — Everything else:
#   pip install -r requirements.txt
#
# CPU-only fallback:
#   pip install torch==2.4.1+cpu --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric==2.6.1
#   pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Core simulation
# ---------------------------------------------------------------------------
mesa==2.1.5
networkx==3.2.1
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2

# ---------------------------------------------------------------------------
# Visualisation
# ---------------------------------------------------------------------------
matplotlib==3.8.2
seaborn==0.13.2
pillow==10.2.0

# ---------------------------------------------------------------------------
# Machine learning
# NOTE: Install torch + torch-geometric manually first (see header above).
#       They are not listed here because pip cannot resolve platform-specific
#       CUDA wheel URLs from a plain requirements.txt.
# NOTE: umap-learn must be imported as `from umap import UMAP` (not `import umap`)
#       to avoid triggering parametric_umap which requires tensorflow.
# ---------------------------------------------------------------------------
umap-learn==0.5.5

# ---------------------------------------------------------------------------
# API backend
# ---------------------------------------------------------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
pydantic==2.5.3

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==9.0.2
pytest-cov==5.0.0

# ---------------------------------------------------------------------------
# Notebooks
# ---------------------------------------------------------------------------
jupyter==1.0.0
ipykernel==6.28.0
