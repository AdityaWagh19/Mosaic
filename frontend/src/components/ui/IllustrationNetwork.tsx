export function IllustrationNetwork() {
  return (
    <img 
      src="/illustrations/hero_network.png" 
      alt="Abstract 3D network graph floating in space" 
      style={{
        display: 'block',
        maxWidth: '100%',
        height: 'auto',
        borderRadius: 'var(--radius-cards)',
        boxShadow: '0 8px 30px rgba(0,0,0,0.04)'
      }}
    />
  );
}
